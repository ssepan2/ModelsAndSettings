Imports Ssepan.Application

Public Class Settings
    Inherits SettingsBase
    Implements ISettings

    '    Public Sub CopyTo(destination As IModelComponent, sync As Boolean) Implements IModelComponent.CopyTo

    '    End Sub

    '    Public ReadOnly Property Dirty As Boolean Implements IModelComponent.Dirty
    '        Get

    '        End Get
    '    End Property

    '    Public Sub Sync() Implements IModelComponent.Sync

    '    End Sub

    '    Public Sub UpdateHandlers() Implements IModelComponent.UpdateHandlers

    '    End Sub

    '    Public Event PropertyChanged(sender As Object, e As System.ComponentModel.PropertyChangedEventArgs) Implements System.ComponentModel.INotifyPropertyChanged.PropertyChanged

    '    Public Function Equals1(other As IModelComponent) As Boolean Implements IEquatable(Of IModelComponent).Equals

    '    End Function

    '#Region "IDisposable Support"
    '    Private disposedValue As Boolean ' To detect redundant calls

    '    ' IDisposable
    '    Protected Overridable Sub Dispose(disposing As Boolean)
    '        If Not Me.disposedValue Then
    '            If disposing Then
    '                ' TODO: dispose managed state (managed objects).
    '            End If

    '            ' TODO: free unmanaged resources (unmanaged objects) and override Finalize() below.
    '            ' TODO: set large fields to null.
    '        End If
    '        Me.disposedValue = True
    '    End Sub

    '    ' TODO: override Finalize() only if Dispose(ByVal disposing As Boolean) above has code to free unmanaged resources.
    '    'Protected Overrides Sub Finalize()
    '    '    ' Do not change this code.  Put cleanup code in Dispose(ByVal disposing As Boolean) above.
    '    '    Dispose(False)
    '    '    MyBase.Finalize()
    '    'End Sub

    '    ' This code added by Visual Basic to correctly implement the disposable pattern.
    '    Public Sub Dispose() Implements IDisposable.Dispose
    '        ' Do not change this code.  Put cleanup code in Dispose(disposing As Boolean) above.
    '        Dispose(True)
    '        GC.SuppressFinalize(Me)
    '    End Sub
    '#End Region

End Class
